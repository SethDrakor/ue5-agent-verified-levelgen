# media/

Placeholder folder. Add before making the repo public:

- 1-2 screenshots of `run_verify()` passing (console output) — the clearest
  proof of the verification loop.
- A screenshot of `test_suite.run_all()` at full pass count.
- A short GIF (10-15s) of a room being generated end-to-end: geometry →
  enemy placement → `run_verify()` → PIE. See `DEMO_VIDEO_SCRIPT.md` in the
  main project for a shot list.

A repo with zero visual proof converts noticeably worse than one with a
single 10-second GIF — worth doing before sharing the link anywhere.
